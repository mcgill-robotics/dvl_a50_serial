dvl_a50_serial:
  ros__parameters:
    port: "/dev/dvl-serial"
    baud_rate: 115200
    frame: "dvl_a50_link"
    rate: 30.0
    enable_on_activate: true
    speed_of_sound: 1500
    led_enabled: true
    mounting_rotation_offset: 0
    range_mode: "auto"

    timeout_configure_ms: 5000
    timeout_reset_dead_reckoning_ms: 5000
    timeout_calibrate_gyro_ms: 20000
    timeout_trigger_ping_ms: 5000
    timeout_set_protocol_ms: 5000
    timeout_get_config_ms: 5000
    
    topic_velocity: "dvl/velocity"
    topic_dead_reckoning: "dvl/dead_reckoning"
    topic_odometry: "dvl/odometry"
